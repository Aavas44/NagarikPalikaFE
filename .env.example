# Backend API base URL (no trailing slash).
# Used for SSR fetches and /api/* rewrites in next.config.js.
API_URL=http://127.0.0.1:4000

# Optional: override glossary JSON directory (default: ./data)
# DATA_DIR=./data
